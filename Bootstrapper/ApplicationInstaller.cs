using System.IO.Compression;

namespace Bootstrapper;

public class ApplicationInstaller
{
    public async Task InstallApplicationVersion(ExecutableWrapper executable, byte[] versionToInstall)
    {
        
    }
}